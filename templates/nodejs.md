# Project Guidelines

## Setup

```bash
npm install
```

## Build & Test

```bash
npm run build
npm test
npm run lint
```

## Code Style

- Use ESLint for linting
- Use Prettier for formatting
- Prefer `const` over `let`; avoid `var`

## Project Structure

- Describe your directory layout here
- Document key entry points

## Testing

- Use the project's test runner (Jest, Vitest, etc.)
- Place tests alongside source files or in `__tests__/`
- Use descriptive test names
